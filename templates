<!DOCTYPE html>
<html>
<head>
    <title>Weather Outfit Advisor</title>
    <style>
        body {
            font-family: Arial;
            background: #f2f7ff;
            text-align: center;
            padding: 40px;
        }
        input, button {
            padding: 10px;
            font-size: 16px;
        }
        .card {
            background: white;
            padding: 20px;
            margin-top: 20px;
            display: inline-block;
            border-radius: 8px;
        }
    </style>
</head>
<body>

    <h1>🌤️ Weather-Based Outfit Advisor</h1>

    <form method="POST">
        <input type="text" name="city" placeholder="Enter city name" required>
        <button type="submit">Get Outfit</button>
    </form>

    {% if weather %}
    <div class="card">
        <h2>📍 {{ weather.city }}</h2>
        <p>🌡️ Temperature: {{ weather.temperature }} °C</p>
        <p>💨 Wind Speed: {{ weather.windspeed }} km/h</p>

        <h3>👕 Outfit Recommendation:</h3>
        <ul>
            {% for item in outfit %}
                <li>{{ item }}</li>
            {% endfor %}
        </ul>
    </div>
    {% endif %}

</body>
</html>
